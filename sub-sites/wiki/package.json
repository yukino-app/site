{
    "name": "@yukino-org/site-wiki",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "mkdocs serve",
        "build": "rimraf ./dist && mkdocs build -c -d ./dist"
    },
    "devDependencies": {
        "rimraf": "^3.0.2"
    }
}
