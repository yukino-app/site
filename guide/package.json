{
    "name": "@yukino-app/guide",
    "productName": "Yukino",
    "version": "0.0.0",
    "scripts": {
        "dev": "cross-env NODE_ENV=\"development\" npm run build && npm run serve",
        "build": "rimraf ./dist && node ./render",
        "serve": "serve dist"
    },
    "dependencies": {
        "cross-env": "^7.0.3",
        "gray-matter": "^4.0.3",
        "marked": "^2.0.7",
        "minify": "^7.0.2",
        "rimraf": "^3.0.2",
        "serve": "^12.0.0"
    }
}
