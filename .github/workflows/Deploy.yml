name: Site (Build)

on:
    workflow_dispatch:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2

            - name: Setup Node.js
              uses: actions/setup-node@v2
              with:
                  node-version: 14.x
                  cache: yarn

            - name: Install dependencies
              run: |
                  yarn install
                  cd ./site yarn install
                  cd ./guide yarn install

            - name: Build Site
              run: yarn build

            - name: Deploy
              uses: JamesIves/github-pages-deploy-action@4.1.0
              with:
                  branch: gh-pages
                  folder: dist
